[project]
name = "sadhan"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Gauresh Tambe", email = "gaureshtambe25@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "ollama>=0.6.2",
    "pyfiglet>=1.0.4",
    "textual>=8.2.8",
]

[project.scripts]
sadhan = "sadhan:main"
sadhan-tui = "sadhan.tui:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
